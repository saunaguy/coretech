# 부하 테스트 분석 최종 보고서

### 조치 1: 데이터베이스 연결 풀 크기 증가
- \`backend/backend_src/db.py\` 파일의 \`create_engine\`에 \`pool_size=50\`, \`max_overflow=100\` 옵션을 추가하여 DB 연결 수를 늘렸습니다.

### 테스트 3: 고부하 테스트 (2차)
- **조건:** 400 동시 사용자, 4000개 요청 (조치 1 적용 후)
- **결과:** **실패**. 3796개 요청 처리 후 동일한 \`Connection reset by peer\` 오류 발생. 이전보다 약 2배의 요청을 더 처리하며 성능은 개선되었으나, 여전히 불안정했습니다.
- **분석:** 백엔드 로그에 특별한 오류가 없었고, \`dmesg\`를 통한 OOM Killer 동작도 확인되지 않았습니다.
- **결론 (가설 2):** 웹 서버(Uvicorn)의 워커 타임아웃(기본값 5초)이 너무 짧아, 부하 시 응답이 늦어지는 연결을 서버가 먼저 끊어버리는 것으로 추정.

### 조치 2: Uvicorn 타임아웃 증가
- \`backend/Dockerfile.backend\` 파일의 Uvicorn 실행 명령어에 \`--timeout-keep-alive 65\` 옵션을 추가하여 연결 유지 시간을 65초로 늘렸습니다.

### 테스트 4: 고부하 테스트 (3차)
- **조건:** 400 동시 사용자, 4000개 요청 (조치 2 적용 후)
- **결과:** **실패**. 3749개 요청 처리 후 클라이언트에서 \`Timeout specified has expired\` 오류 발생. 서버가 응답하지 못하는 현상은 동일했습니다.

## 4. 권장 해결 방안 (미적용)

\`Dockerfile.backend\`에서 \`gunicorn\`과 같은 프로세스 매니저를 사용하여 Uvicorn 워커를 여러 개(예: 8개) 실행하도록 설정을 변경해야 합니다. 이를 통해 서버의 모든 CPU 코어를 활용하여 부하를 분산시킬 수 있습니다.

\`\`\`dockerfile
# 예시: Gunicorn을 사용한 최종 수정 제안
CMD ["gunicorn", "-w", "8", "-k", "uvicorn.workers.UvicornWorker", "backend_src.main:app", "--bind", "0.0.0.0:8000", "--timeout", "65"]
\`\`\`

## 1. 개요

Docker로 배포된 \`coretech\` 애플리케이션의 성능 한계점을 파악하고, 원인을 분석 및 해결하는 것을 목표로 부하 테스트를 진행했습니다.

## 2. 테스트 및 분석 과정

### 테스트 1: 기준 성능 측정
- **조건:** 100 동시 사용자, 1000개 요청
- **결과:** **성공**. 초당 약 49개의 요청을 처리하며, 시스템이 안정적임을 확인했습니다.

### 테스트 2: 고부하 테스트 (1차)
- **조건:** 400 동시 사용자, 4000개 요청
- **결과:** **실패**. 1912개 요청 처리 후 \`Connection reset by peer\` 오류 발생.
- **분석:** 백엔드 로그에서 \`sqlalchemy.exc.TimeoutError\`를 발견했습니다.
- **결론 (가설 1):** 데이터베이스 연결 풀(Connection Pool)의 크기가 너무 작아, 동시 요청을 감당하지 못하고 연결 시간 초과 발생.

## 3. 최종 원인 분석 (sar 모니터링)

마지막 테스트와 동시에 시스템 자원 모니터링 도구 \`sar\`를 실행하여 결정적인 증거를 확보했습니다.

- **\`sar\` 분석 결과:** 테스트 중 서버의 **CPU 유휴(idle) 상태가 평균 81.5%**를 기록했습니다. 이는 **서버의 CPU 자원이 전혀 부족하지 않았음**을 의미합니다.
- **최종 결론:** 하드웨어 문제가 아닌, **소프트웨어 설정 문제**였습니다. 백엔드 웹 서버 Uvicorn이 **단일 워커(Single Worker) 프로세스**로 실행되고 있었습니다. 이 때문에 서버의 8개 CPU 코어 중 단 1개만 사용되었고, 400명의 동시 요청이 이 하나의 프로세스에 몰리면서 병목 현상이 발생하여 시스템 전체가 응답 불능에 빠진 것입니다.
