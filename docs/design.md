# 초기 설계 문서

- 프론트엔드: React (점진 도입)
- 백엔드: FastAPI (점진 도입)
- 데이터 저장: SQLite → 향후 Postgres 확장
- 배포: Docker Compose (선택)

결정 로그는 `logs/YYYY-MM-DD.jsonl`에 축적하며, 주요 결정은 GitHub Issue(Decision 템플릿)로도 기록합니다.

