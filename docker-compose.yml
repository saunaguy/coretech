services:
  db:
    image: postgres:16-alpine
    container_name: coretech-db
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-coretech}
      POSTGRES_USER: ${POSTGRES_USER:-coretech}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-coretech}
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      # - "5432:5432"  # expose if you want local psql access
      - "5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile.backend
    container_name: coretech-backend
    environment:
      SECRET_KEY: ${SECRET_KEY:-change-this-in-production}
      JWT_EXPIRE_MINUTES: ${JWT_EXPIRE_MINUTES:-1440}
      DATABASE_URL: ${DATABASE_URL:-postgresql+psycopg://coretech:coretech@db:5432/coretech}
      # CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:3000,http://127.0.0.1:3000}
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile.frontend
      args:
        # Pass API_BASE_URL into the image build so Next.js can embed it
        API_BASE_URL: ${API_BASE_URL:-http://localhost:8000}
    container_name: coretech-frontend
    environment:
      NODE_ENV: production
      # Public base URL the browser will call
      NEXT_PUBLIC_API_BASE_URL: http://backend:8000
      INTERNAL_API_BASE_URL: http://backend:8000
    ports:
      - "3000:3000"
    depends_on:
      - backend

volumes:
  pgdata:
